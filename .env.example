# FitTrack Environment Configuration
# Preimenovaj to datoteko v .env in spremeni vrednosti

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=fittrack

# Application Settings
APP_DEBUG=false
APP_ENV=production

# Session Settings
SESSION_LIFETIME=3600

# Security
HASH_ALGO=bcrypt
